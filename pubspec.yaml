name: jetleaf_core
description: The core runtime that powers JetLeaf’s pod, context, and app lifecycle.
version: 1.0.5

homepage: https://jetleaf.hapnium.com
repository: https://github.com/jetleaf/jetleaf_core
issue_tracker: https://github.com/jetleaf/jetleaf_core/issues
documentation: https://jetleaf.hapnium.com/docs/core

topics:
  - ioc
  - jetleaf
  - context
  - lifecycle
  - application-core

keywords:
  - ioc
  - jetleaf
  - service-locator
  - context
  - pod

environment:
  sdk: ^3.9.0

dependencies:
  meta: ^1.17.0
  jetleaf_lang: ^1.0.5
  jetleaf_logging: ^1.0.5
  jetleaf_convert: ^1.0.5
  jetleaf_utils: ^1.0.5
  jetleaf_env: ^1.0.5
  jetleaf_pod: ^1.0.5

dev_dependencies:
  test: ^1.21.0
  lints: ^6.0.0